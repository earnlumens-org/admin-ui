<!--
  /settings layout.

  Renders the matched child route only. The hub (settings/index.vue)
  shows the tenant context + category links; each child page focuses
  on a single configuration domain so the owner can work on one thing
  at a time without sibling cards stealing attention.
-->
<template>
  <router-view />
</template>
